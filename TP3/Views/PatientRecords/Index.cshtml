@model IEnumerable<TP3.Models.PatientRecord>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Patient.LastNamePatient)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PatientHistory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.LastNamePatient)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PatientHistory)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PatientRecordID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PatientRecordID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PatientRecordID })
        </td>
    </tr>
}

</table>
